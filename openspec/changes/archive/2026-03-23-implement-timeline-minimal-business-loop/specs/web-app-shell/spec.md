## ADDED Requirements

### 需求:Space 页面必须展示 Timeline 列表区域
系统必须在现有 Space 页面壳层能力上补充 Timeline 列表区域，以便用户在同一页面内同时看到内容单元和最近变化。

#### 场景:进入某个 Space 后查看最近变化
- **当** 用户从 Space 列表进入某个具体 Space
- **那么** Space 详情页必须展示该 Space 下最近的 TimelineEvent 列表
- **并且** 每个列表项必须至少展示事件类型、关联对象标题、事件时间和可展示的事件摘要
