## ADDED Requirements

### 需求:Space 详情页必须承载 Timeline 区域
系统必须在 Space 详情页 / Space 首页中承载该 Space 的 Timeline 区域，以便用户看到最近发生的关键变化。

#### 场景:进入某个 Space 后查看 Timeline
- **当** 用户进入某个具体 Space 的详情页
- **那么** 页面必须展示该 Space 的 Timeline 列表区域
- **并且** 该区域必须能够展示最近发生的关键 TimelineEvent
