## 1. 数据模型

- [x] 1.1 为 `Node` 增加稳定顺序字段，并通过 Prisma 迁移为现有数据回填顺序值
- [x] 1.2 新增 `NodeRelation` 数据模型与仓储约束，支持创建、删除和按 Space 查询关系

## 2. 后端能力

- [x] 2.1 调整 Node 列表与 Graph 数据接口，统一按稳定顺序返回节点与布局
- [x] 2.2 新增 NodeRelation API，支持创建连线、删除连线和校验重复/无效关系
- [x] 2.3 停止 Graph View 使用 Timeline 推导边，改为返回显式节点关系

## 3. 前端交互

- [x] 3.1 在 Graph View 中接入按顺序展示后的节点位置，确保新节点默认出现在末尾
- [x] 3.2 在画布中支持删除连线和拖拽新建连线，并调用新的关系接口持久化
- [x] 3.3 验证空图、单节点、多节点场景下的顺序和连线行为可用
