## 1. Space 主视图收敛

- [x] 1.1 盘点现有 Space 页面中可复用的“当前进展节点”数据来源，并确定首版默认主视图的取值规则
- [x] 1.2 调整 `SpaceDetailContainer` 的页面编排，使进入 Space 时默认先展示当前进展节点主视图
- [x] 1.3 将 Space 详情信息改为次级入口承载，而不是默认占据首屏

## 2. 次级信息按需展开

- [x] 2.1 调整节点详情交互，使节点详情默认隐藏并通过显式入口展开
- [x] 2.2 调整最近变化入口与承载方式，使其默认不与主视图并列占据首屏
- [x] 2.3 保留 Graph View 入口，但确保其作为二级视图而不是默认主表达

## 3. 验证

- [x] 3.1 验证进入 Space 时首屏聚焦当前进展节点，且页面没有默认并列展开多个次级信息区
- [x] 3.2 验证 Space 详情、节点详情和最近变化仍可通过显式入口访问
- [x] 3.3 验证中英文环境下文案、入口和默认显隐行为一致可用
